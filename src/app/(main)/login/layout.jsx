export const metadata = { title: "Sign In | Govt. Primary School" };

// Login page gets its own minimal layout (no public Navbar/Footer)
export default function LoginLayout({ children }) {
  return <>{children}</>;
}
