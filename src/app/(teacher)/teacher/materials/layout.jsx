export const metadata = {
  title: 'Study Materials - Teacher Portal',
  description: 'Share PDFs, reference links, lecture notes, and resource attachments with students.'
};

export default function MaterialsLayout({ children }) {
  return children;
}
