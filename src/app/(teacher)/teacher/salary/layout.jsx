export const metadata = {
  title: 'Salary Ledger History - Teacher Portal',
  description: 'Track monthly basic pays, credited allowances, tax deductions, and payment status.'
};

export default function SalaryLayout({ children }) {
  return children;
}
