export const metadata = {
  title: "Notices | FIT Campus",
  description: "Explore Notices page at Fontana Institute of Technology (FIT).",
};

export default function NoticesLayout({ children }) {
  return <>{children}</>;
}
