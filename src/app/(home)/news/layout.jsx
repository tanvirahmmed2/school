export const metadata = {
  title: "News | FIT Campus",
  description: "Explore News page at Fontana Institute of Technology (FIT).",
};

export default function NewsLayout({ children }) {
  return <>{children}</>;
}
