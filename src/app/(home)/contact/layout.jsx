export const metadata = {
  title: "Contact | FIT Campus",
  description: "Get in touch with Fontana Institute of Technology (FIT) for support, inquiries, and admissions.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
