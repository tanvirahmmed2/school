export const metadata = {
  title: "Administration | FIT Campus",
  description: "Explore Administration page at Fontana Institute of Technology (FIT).",
};

export default function AdministrationLayout({ children }) {
  return <>{children}</>;
}
