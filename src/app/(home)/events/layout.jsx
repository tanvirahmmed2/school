export const metadata = {
  title: "Events | FIT Campus",
  description: "Explore Events page at Fontana Institute of Technology (FIT).",
};

export default function EventsLayout({ children }) {
  return <>{children}</>;
}
