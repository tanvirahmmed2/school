export const metadata = {
  title: "Recognitions | FIT Campus",
  description: "Explore Recognitions page at Fontana Institute of Technology (FIT).",
};

export default function RecognitionsLayout({ children }) {
  return <>{children}</>;
}
